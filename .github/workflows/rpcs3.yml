name: Build RPCS3

on:
  workflow_call:
  push:

jobs:
  build-rpcs3:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/anyvm-org/anyvm:latest
    steps:
    - name: Setup BSD
      run: |
        python3 /anyvm.org/anyvm.py --os freebsd
    - name: clone repo
      run: |
        pwd
        ls -al
        ls -al /
