name: Build RPCS3

on:
  workflow_call:
  push:

jobs:
  build-rpcs3:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/anyvm-org/anyvm:latest
    steps:
    - name: test
      run: |
        python3 anyvm.py --os freebsd -- uname -a
